# Security Skill

Use this skill whenever code touches secrets, network requests, or user data.

Claude responsibilities:
- keep secrets server-side
- prevent client exposure of API keys
- ensure graceful fallback paths
- avoid depending on external services for core gameplay

Rule:
If a service can fail, the game must still be playable.
